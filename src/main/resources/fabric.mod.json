{
	"schemaVersion": 1,
	"id": "${modid}",
	"version": "${version}",
	"name": "zenith attributes null fix",
	"description": "Adds a null check to a specific Porting-Lib fix in Zenith Attributes that shouldnt happen",
	"authors": [
		"Linguardium"
	],
	"license": "MIT",
	"environment": "*",
	"mixins": [
		"zenithattributesnullfix.mixins.json"
	],
	"depends": {
		"fabricloader": ">=${loader_version}",
		"minecraft": "~${minecraft_version}",
		"java": ">=17"
	}
}